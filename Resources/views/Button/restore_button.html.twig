{#<a
	href="{{ path('cogitoweb_database_backup_restore', {pathname: file.pathname}) }}"
	class="btn"
	title="{{ 'list.restore_button' | trans({}, 'CogitowebDatabaseBackupBundle') }}"
>
	<i class="glyphicon glyphicon-trash"></i>
</a>#}
<button
	type="button"
	class="btn"
	data-toggle="modal"
	data-target="#confirm-restore"
	data-pathname="{{ file.pathname }}"
	data-mtime="{{ file.mtime }}"
	title="{{ 'list.restore_button' | trans({}, 'CogitowebDatabaseBackupBundle') }}"
>
	<i class="glyphicon glyphicon-open"></i>
</button>